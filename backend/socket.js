/**
 * socket.js — REMOVED (migrated to Cloudflare Workers)
 *
 * Real-time notifications are now handled via Server-Sent Events (SSE).
 * See: routes/notifications.js → GET /api/notifications/stream
 * Frontend: src/hooks/useSocket.js (uses EventSource, not socket.io)
 */
